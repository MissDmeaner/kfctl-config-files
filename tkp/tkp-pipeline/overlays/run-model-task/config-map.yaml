apiVersion: v1
kind: ConfigMap
metadata:
  name: run-model
data:
  run-model.sh: |-
    #!/usr/bin/env bash
    python simple-model.py
    if (( $? == 0 )); then
      tensorboard --logdir /kubeflow/logs
    else
      echo 'run model failed'
    fi
